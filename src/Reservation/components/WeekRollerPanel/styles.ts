import { css } from '@emotion/core'

const weekTabContainer = css`
  display: flex;
  justify-content: space-between;
  padding: 0 7px;

  > span {
    flex-grow: 1;
  }
`

const panel = css``

export { panel, weekTabContainer }
