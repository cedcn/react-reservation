import { css } from '@emotion/core'

const cellStatus = css`
  position: absolute;
  bottom: 5%;
  left: 50%;
  transform: translateX(-50%);
  font-size: 12px;
  width: 100%;
`

export { cellStatus }
